interface:
  display_name: "Repository Governance"
  short_description: "Audit and maintain durable repository agent guidance"
  default_prompt: "Audit, bootstrap, evaluate, or minimally update repository-level AGENTS.md using current repository evidence and durable governance rules."

policy:
  allow_implicit_invocation: false
